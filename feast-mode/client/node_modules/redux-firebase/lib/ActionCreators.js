'use strict';

exports.__esModule = true;
exports.subscribe = subscribe;
exports.sync = sync;
exports.unsubscribe = unsubscribe;

var _ActionTypes = require('./ActionTypes');

var _reduxActions = require('redux-actions');

var doSubscribe = _reduxActions.createAction(_ActionTypes.PATHS_SUBSCRIBED);

function subscribe(sid, paths) {
  return doSubscribe({ paths: paths, sid: sid });
}

var doSync = _reduxActions.createAction(_ActionTypes.PATHS_SYNCED);

function sync(updates) {
  return doSync(updates);
}

var doUnsubscribe = _reduxActions.createAction(_ActionTypes.PATHS_UNSUBSCRIBED);

function unsubscribe(sid, paths) {
  return doUnsubscribe({ paths: paths, sid: sid });
}